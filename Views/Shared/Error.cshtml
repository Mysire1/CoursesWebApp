@model CoursesWebApp.Controllers.ErrorViewModel
@{
    ViewData["Title"] = "Помилка";
}

<h1 class="text-danger">Помилка.</h1>
<h2 class="text-danger">Під час обробки вашого запиту сталася помилка.</h2>

@if (Model?.ShowRequestId == true)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Що можна зробити?</h3>
<p>
    Поверніться на <a href="@Url.Action("Index", "Home")">головну сторінку</a> або спробуйте пізніше.
</p>